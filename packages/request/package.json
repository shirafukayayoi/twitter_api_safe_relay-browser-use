{
	"name": "twitter-api-safe-request",
	"version": "0.0.2",
	"description": "Safe request helpers for Twitter/X web API access through a browser page bridge.",
	"type": "module",
	"main": "dist/index.js",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"development": "./src/index.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"injects",
		"README.md"
	],
	"publishConfig": {
		"types": "./dist/index.d.ts",
		"exports": {
			".": {
				"types": "./dist/index.d.ts",
				"import": "./dist/index.js"
			}
		}
	},
	"scripts": {
		"clean": "rimraf dist",
		"dev": "tsc -p tsconfig.json --watch",
		"build": "pnpm clean && tsc -p tsconfig.json",
		"test": "vitest run",
		"type-check": "tsc -p tsconfig.json --noEmit"
	},
	"keywords": [
		"twitter",
		"x",
		"browser",
		"api"
	],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@twitter-api-safe/test-utils": "workspace:*",
		"@types/node": "^25.0.2",
		"playwright": "^1.60.0",
		"rimraf": "^6.1.3",
		"typescript": "^5.9.3",
		"vitest": "^4.1.5"
	}
}
